import styled from 'styled-components/native';

const SectionTitle = styled.Text`
  font-weight: 800;
  font-size: 22px;
  color: #000000;
  margin-top: 22px;
  padding: 10px 20px;
`;

export default SectionTitle; // Clean export
